# 当前讨论断点

更新时间：2026-05-08

## 当前主线

正在按“从头重新讨论整个公式”的方式，逐项审查东方财富通主公式。

当前讨论位置：风险提示模块。

最新讨论到：风险模块中 `高位放量创新高风险` 小块，已讨论并暂时接受 `高险近高N / 高险涨N / 高险涨幅线`、`高险前高已成`、`高险当前续高`、`高险已有涨幅`。正在讨论 `高险趋势仍强:=C>MID AND C>短线 AND PCTB>0.75;`。

插入事项：已阅读外部 `沟通记录2.docx`，其最重要价值是暴露 `扩张保护起点 / 初扩早期险过滤` 可能反复复用旧收缩背景、误挡高位放量创新高冲回硬风险。已把相关方法写入 `pending-verification-list.md` 和 `collaboration-guidelines.md`。当前主线仍按本项目方法继续推进，不直接照搬外部文档公式。

## 最近已暂时接受

- 周期识别：日线、周线、月线保留；非这三个周期后续只通过最终显示许可过滤。
- `BBN / KCN / MAN`：暂时接受，后续验证是否导致信号偏慢或噪音变化。
- `P / KCP / 深限`：暂时接受，后续重点验证极上识别和挤压频率。
- `TN / TM / WM / WRP`：暂时接受，后续专项验证趋势是否滞后、带宽参考是否滞后、`WRP=0.85` 是否过早压制信号。
- `SQN`：暂时接受新版 `SQN:=IF(周线,3,IF(月线,3,5));`，后续验证月线信号是否变多、旧挤压是否被宽度许可有效过滤。
- `VN / VP / BVP`：暂时接受原值，列为高优先级验证项，重点看放额是否太容易成立、巨额风险是否识别准确、不同股票量能波动是否差异过大。
- `BP / YP`：暂时接受原值，后续验证 `BP=0.45` 是否误伤温和有效启动、`YP=0.45` 是否误伤强势换手突破。`中阳无长上` 暂不进主公式，进入验证副图和待验证清单。
- `BRN / FN`：暂时接受原值，后续验证 `BRN` 是否让 `构上` 太难或太容易成立，`FN=5` 是否对日线震荡型启动过早标败。`上败弱化` 作为候选验证项保留，不进主公式。
- 黄色冷却三项：暂时接受原值，列为中高优先级验证项，重点看是否压制有效二次黄信号，以及 `近上轨信号`、`强趋势运行` 是否过度压制普通黄。
- 败/速/冲冷却参数：暂时接受原值，后续验证 `速后N=12` 是否压制有效冲、`冲冷却N=8` 是否压制有效二次平台突破。`速后强运行` 作为候选优化保留，不进主公式。
- 布林带与 `PCTB`：接受保留不改，`PCTB` 继续作为核心强弱坐标。
- Keltner 通道与挤压：通道和挤压定义接受保留；`近挤 / 效挤` 采用新版逻辑。由于 `近挤` 缩短，`迟发挤压背景` 是防漏关键模块。
- 布林带宽度基础状态：带宽、长短均宽、高宽、低宽接受保留；有效宽均/高宽/低宽作为防异常兜底保留，后续验证是否受极端值和波动切换滞后影响。
- 前有效参考与宽比：计算逻辑接受保留；已确认整理版删除 `前宽比2`，统一使用 `前宽比`。
- 高宽判断：绝对高宽线、相对高宽线、今天+上一根判断接受保留，后续验证阈值是否过松/过敏，以及是否需要观察上上根。
- 持宽判断：`宽近高 / 宽速率 / 硬高宽 / 软持宽 / 持宽` 接受保留，列为高优先级验证项；重点验证 `WRP=0.85` 是否过敏、`软持宽` 是否误伤强势二次机会。
- 收缩识别参数：`缩窄N / 缩幅比例 / 低位均比 / 低位低比` 暂时接受；月线 `缩幅比例=0.92` 暂不调整，后续结合压缩低位和真正收缩验证是否偏松。
- 实际收缩判断：`带宽高点前 / 带宽低点近 / 收缩幅度 / 压缩低位` 暂时接受；`压缩低位` OR 逻辑接受，后续验证是否偏宽。
- 收缩合成：`有效缩窄 / 收缩过程 / 真正收缩` 结构接受；`有效挤压` 使用新版。`近窗缩幅` 进入验证副图，不进主公式。
- 启动前带宽环境：核心逻辑接受，整理版删除未使用的 `启动前相低`。
- 启动前宽度许可：暂时保留，后续验证和整理版检查 `启动前绝宽高 / 启动前相宽高 / 启动前持宽` 是否冗余；验证副图加入 `启动前高宽未被持宽覆盖`。
- K线质量计算：主公式保留 `K幅 / 实体 / 实占 / 上影 / 下影 / 阳强 / 长上`；整理版删除未使用的 `阴强 / 长下`，但验证副图保留 `下影 / 长下`。
- 成交额模块：整理版采用 `AMT0 / 有额` 防空新版，保留 `缩额`；`额比 / 近高额 / 近高额比 / 前高K额比较` 进入验证副图和回测观察，不进主公式硬过滤。
- 结构突破：主公式保留 `构上 / 构下`，验证副图增加 `价高突破 / 价低跌破`。
- 趋势多头模块：主公式保留 `长线 / 短线 / 长向 / 短向 / 强多 / 初多`；整理版删除未使用的 `强空 / 初空`；`短向` 固定3根列为后续验证项。
- 有效上穿1.0：主公式保留 `有效上穿1:=CROSS(PCTB,1) AND NOT(浅绕回上穿)`；后续重点验证被 `浅绕回上穿` 过滤的强势浅回踩再突破，是否由 `续扩 / 速 / 冲 / 确` 补位。
- 普通上轨突破背景：接受在 `突破背景` 中加入 `迟发挤压背景`，其余暂不动。
- 温上承接不足过滤：最新决定为暂不加入主公式；`温上承接弱` 作为候选验证项，观察跳空失守、阴线低收弱、放量滞涨弱是否能过滤噪音且不误伤启动。
- 温上质量收紧：接受 `温上:=温上基 AND (构上 OR (放额 AND 价推进) OR (温额 AND 阳强)) AND NOT(长上)`；`慢推温上` 暂作独立候选观察。
- 暴上与极上：主逻辑暂时保留；`强收中阳` 先验证不替换 `阳强`；重点验证 `巨额` 是否过早把真强启动归为 `极上险`。
- 中位收缩背景/中位收缩突破：暂时保留；后续验证是否偏宽，以及 `阳强` 是否漏掉 `强收中阳`。
- 中位温上/中位暴上/中位突破保护：接受分界统一为 `1.10`，并将 `中位突破保护` 简化为 `中位收缩突破`。
- 带宽初扩：暂时保留不改；后续验证是否太慢或太敏感，尤其日线是否慢一根、月线是否过早。
- 平台窗口与平台高低点：暂时保留；后续重要验证固定窗口是否过死，以及平台高/低点是否被异常影线污染。
- 平台前涨幅与平台未大涨：暂时保留；后续验证固定起点是否失真、涨幅限是否适配不同波动股票，以及是否误伤强势中继平台。
- 平台振幅边界：不为少数高波动股票全局放宽主公式平台振幅；高波动平台后续考虑分层验证或另做辅助公式。
- 平台压缩整理：暂时保留；后续验证第二条路径是否偏宽，和 `平台窄幅整理` 是否互补有效。
- 平台前热数/平台前未热：暂时保留；后续验证是否过严或过松，尤其日线是否放过高热平台、月线是否误伤有效平台。
- 平台宽不高：暂时保留；后续验证是否与 `启动前持宽` 冗余，以及是否误伤强势中继平台。
- 平台高热状态：暂时保留；后续重点验证是否过严、OR条件哪条最常压制，以及是否误伤强势中继平台。
- 平台突破/平台前强趋势/高位平台再突：暂时保留；后续验证平台突破是否过严、平台前强趋势是否误判健康中继平台。
- 平台普通强启：已确认删除 `AND (放额 OR 构上)` 和 `AND NOT(巨额 AND 明显长上)` 两个重复条件；后续仍验证哪个条件最常压制。
- 平台极上强启：已确认整理版删除 `AND (放额 OR 构上)`，因为 `平台突破` 已经包含 `构上`；已确认删除 `AND NOT(巨额 AND 明显长上)`，因为 `NOT(长上)` 已覆盖 `明显长上`；`PCTB<1+深限*2.20` 暂时保留并列入重点验证。
- 跳空起爆平台：已确认作为独立通道保留，用于补充跳空高开后继续收强、突破平台的起爆情形；已确认保留 `AND (放额 OR 构上)`，因为该通道没有直接包含 `平台突破`，`构上` 不必然成立；已确认删除 `AND NOT(巨额 AND 明显长上)`，因为 `NOT(长上)` 已覆盖 `明显长上`；跳空后承接质量暂不改主公式，加入验证副图。
- 冲候选预与冲首次：合并方式接受；`冲首次:=冲候选预 AND NOT(REF(冲候选预,1))` 接受保留，后续验证是否压掉有效第二天延续突破；`近冲首次` 接受保留，继续验证 `冲冷却N` 是否压掉有效二次平台突破；`冲有效` 接受保留，继续验证 `速后运行 / 平台高热状态` 是否过度压制有效冲。
- 蓝黄源头合并：`温上源:=温上 OR 中位温上;`、`暴上源:=暴上 OR 中位暴上;` 接受保留，验证副图需要追踪普通来源和中位来源。
- 蓝压缩质量：接受保留；后续验证 `启动前宽比<1.05` 是否过严、蓝黄分层是否合理。
- 蓝挤压背景：确认修改为 `蓝挤压背景:=有效挤压;`，以继承新版有效挤压和挤压宽许可。
- 蓝收缩背景：暂时保留；后续验证 `中位收缩突破` 单独进入蓝背景的质量。
- `优质黄背景`：确认改名为 `蓝优质背景`。
- 昨收缩突破背景：确认新增 `昨收缩突破背景:=REF(真正收缩,1);`，并纳入 `蓝优质背景`，用于补足昨天真正收缩、今天突破但今天不再满足收缩过程的优质信号。
- 蓝优质背景总过滤：确认统一保留 `AND 蓝压缩质量`。
- 蓝黄源头分配：接受保留，蓝/黄核心区别是 `蓝优质背景` 背景质量分层。
- 蓝黄信号源合并：接受保留，验证副图需要拆出蓝温/蓝暴/黄温/黄暴。
- `优质黄候选原`：确认改名为 `蓝候选原`，逻辑不变。
- 黄可显示占用原：确认替代原 `强普通黄候选原` 进入冷却占用；新增 `强收中阳黄可显示占用` 作为验证项，观察强收中阳是否也应参与黄色冷却占用。
- 冷却占用信号原：确认将 `蓝候选原` 改为 `蓝有效`，并用 `黄可显示占用原` 替代 `强普通黄候选原` 进入冷却占用，避免未通过有效过滤或明显不具备可显示资格的候选占用黄色冷却。
- 近上轨信号：接受保留。
- 黄段结束：接受保留；短线为日13、周8、月6均线。
- 近段已结束：接受保留。
- 黄有效再收缩：接受保留；后续验证副图拆分四条触发路径。
- 黄有效再收缩新鲜度：确认保持严格版本，只看上一根K线，不放宽到最近2-3根。
- 强趋势运行：确认新增 `近期仍强`，避免旧强势集中在窗口初期却仍压制黄色。
- 距上轨信号/时间解禁：接受保留。
- 时间回落重置：接受保留。
- 黄色显示许可：接受保留。
- 近期强推：确认将 `蓝候选原` 改为 `蓝有效`。
- 黄色充分重置：接受保留。
- 黄色新鲜许可：接受保留。
- 蓝显示许可：确认删除，因未被后续有效层或显示层引用。
- 蓝温有效/蓝暴有效：接受保留。
- 黄温有效/黄暴有效：接受保留。
- 蓝有效/黄有效/有效启动信号预：接受保留。
- 布林滞后保护参数 `滞后强N`：接受保留。
- 趋势健康：接受保留；`早期趋势健康 / 趋势健康扩展` 进入验证副图观察，不进主公式。
- 价格继续强：接受保留。
- 布林跟随扩张：接受保留。
- PCTB伪弱：接受保留，`PCTB>0.75` 后续验证。
- PCTB高位惯性：接受保留，并删除重复条件 `AND NOT(巨额 AND 明显长上)`。
- 布林滞后保护：接受保留。
- 确认强区：接受保留，逻辑为 `确认强区:=PCTB>0.90 OR 布林滞后保护;`。后续验证依靠布林滞后保护而非直接 `PCTB>0.90` 进入确认强区的确认信号是否有效。
- 确认条件拆分：接受不再照搬原 `确认条件` 作为所有确认信号共用条件；改为先定义 `确认基础条件`，再分别讨论 `确2/确3/确4` 的专用条件，避免 `NOT(长上)` 等公共条件与后续专用规则冲突。
- 确2实体高突破：接受新增 `C>确2前两实体高`，要求确认日收盘高于启动日和昨日的实体高点，使 `确2` 从连续收盘抬高升级为突破前两根实体压力。
- 确2长上处理：接受 `确2` 不加入 `NOT(长上)`；验证副图记录 `确2长上警:=确2 AND 长上`，作为确认当天有长上影瑕疵的提示，不直接取消 `确2`。
- 确2量能/结构：接受继续使用 `温额 OR 构上`，不升级为 `放额 OR 构上`；验证副图拆分 `确2放额`、`确2温额非放`、`确2构上`。
- 确2强区路径：接受不要求启动后到确认前曾经 `PCTB>0.90`；验证副图记录 `确2前曾强区` 和 `确2靠滞后保护`，用于观察靠布林滞后保护进入确认强区的 `确2` 是否有效。
- 确3基础结构：接受用 `确3前三实体高:=REF(HHV(实体高,3),1);`，要求今天收盘高于今天之前三根K线实体高点；不能用 `HHV(实体高,3)` 直接比较，因为会把今天自己算进去。
- 确3中间过程：接受不要求启动后第1、2天逐日收盘抬高；`确3` 用来补充启动后中间震荡消化、第三天重新突破前三实体高的确认。
- 确3长上处理：接受 `确3` 不加入 `NOT(长上)`；验证副图记录 `确3长上警:=确3 AND 长上`，作为确认当天有长上影瑕疵的提示，不直接取消 `确3`。
- 确3量能/结构：接受继续使用 `温额 OR 构上`，不升级为 `放额 OR 构上`；验证副图拆分 `确3放额`、`确3温额非放`、`确3构上`。
- 确3强区路径：接受不要求启动后到确认前曾经 `PCTB>0.90`；验证副图记录 `确3前曾强区` 和 `确3靠滞后保护`。
- 确4基础结构：接受用 `确4前四实体高:=REF(HHV(实体高,4),1);`，要求今天收盘高于今天之前四根K线实体高点；不能用 `HHV(实体高,4)` 直接比较，因为会把今天自己算进去。
- 确4中间未坏：接受新增 `确4中间未坏:=REF(COUNT(C<MID OR PCTB<0.65,3),1)=0;`，用于过滤启动后中间三根明显走弱再反抽的情况。
- 确4长上处理：接受正式 `确4` 加入 `NOT(长上)`；验证副图记录 `确4长上候选`，用于观察被长上过滤掉的候选是否确实较弱。
- 确4量能/结构：接受继续使用 `温额 OR 构上`，不升级为 `放额 OR 构上`；验证副图拆分 `确4放额`、`确4温额非放`、`确4构上`。
- 确4强区路径：接受不要求启动后到确认前曾经 `PCTB>0.90`；验证副图记录 `确4前曾强区` 和 `确4靠滞后保护`。
- 险反：接受保留原逻辑，不放宽到最近2日风险；验证副图记录 `近2日风险反包候选`。
- 确认最终合成：接受改为 `确:=确2 OR 确3 OR 确4 OR 险反;`。
- 普通黄后的确认：接受暂不加入主公式；后续设计 `黄后确认候选` 进入验证副图和回测观察。
- 已有上轨行为：接受暂时保留；验证副图需显示 `已有上轨行为`、`首扩未走出`、`首扩周期许可`、`上轨实际运行数`，检查其是否过早成立。
- 续热险中的带宽初扩：接受暂时保留；验证副图对照 `带宽初扩` 与 `高宽风险环境`，检查是否误伤健康早扩或漏掉高宽续热。
- 续热险PCTB区间：接受暂时保留 `PCTB>=1.15 AND PCTB<1+深限`；后续验证日线是否太敏感、月线区间是否太窄，以及极上区间是否由后续风险模块承接。
- 续热险触发源：接受保留 `(长上 OR 巨额)`；验证副图拆分 `续热长上源`、`续热巨额源`、`续热纯巨额源`，重点观察单纯巨额触发是否偏敏感。
- 高宽风险环境参数：接受暂时保留 `高宽N/高宽阈`，即日线10/5、周线5/3、月线3/2；后续验证持续高宽判断是否合理。
- 宽带持续高：接受 `宽带绝对持续高 OR 宽带相对持续高` 结构保留；验证副图拆分绝对路径和相对路径，观察是否过敏。
- 高宽风险环境：接受保留 `宽带持续高 AND NOT(扩张保护期)`；下一步重点检查 `扩张保护期` 是否过宽或过窄。
- 扩张保护期：接受保留 `保护N / 扩张保护起点 / 扩张保护期` 原结构；新增 `保护期弱化候选:=扩张保护期 AND (长上 OR C<REF(C,1) OR 滞源 OR 慎源)` 作为后续验证项，不进主公式。
- 协作规范新增：区分“可直接优化”的逻辑问题和“需要后续验证”的不确定问题，不能习惯性全部保留主公式再验证。

## 当前待确认

下次继续讨论：

```pascal
启后保护N:=IF(周线,2,IF(月线,1,3));
启后早期:=BARSLAST(首扩启)>0 AND BARSLAST(首扩启)<=启后保护N;
启后趋势未坏:=C>短线 AND C>MID AND PCTB>0.75;
启后非极热:=PCTB<1+深限 AND NOT(高宽风险环境);
启后冲高回落保护:=启后早期 AND 启后趋势未坏 AND 启后非极热;
```

待讨论：继续拆 `启后冲高回落保护`。

## 当前插问与审计

用户指出：上下文很长，助手应该主动提醒并执行上下文项目化，而不是等用户提出。

处理结论：

- 新增本文件 `current-checkpoint.md`，专门记录当前断点。
- 后续每完成一个大模块，或发生较长插问、上下文可能混乱时，助手应主动更新本文件。
- 助手需要反省并改进主动性：不能只在用户问到“上下文是否过长”时才提出项目化方案。
- 用户进一步要求：后续在不影响沟通质量和效率的前提下，主动减少 token 消耗。
- 已写入 `collaboration-guidelines.md`：用项目文件和短状态承接长期上下文，少重复已确认内容；但不能牺牲公式中文解释、影响分析和待验证记录。
- 用户指出：选中文本后点击“添加到对话”，界面不一定显式显示引用原文，导致回溯时不知道当时引用了什么。
- 已写入 `collaboration-guidelines.md`：以后基于选中文本追问时，助手先简短复述被追问的关键引用，再回答，保证聊天记录可追溯。
- 用户指出：不希望总是用户提醒后，助手才发现重要情况没考虑。
- 已写入 `collaboration-guidelines.md`：新增“主动盲点扫描”，以后讨论公式条件时，助手要主动检查次数/幅度/过程/位置/极端值/OR过宽/AND过严/旧背景误用/二次机会误伤等边界情况。
- 用户指出：“是否需要自适应参考”这个思维很好，应推广到各环节。
- 已写入 `collaboration-guidelines.md`：以后遇到固定窗口、固定阈值、固定倍数时，助手要主动考虑是否需要根据周期、波动状态、历史分位、长短参考差异、趋势/压缩/高宽阶段做自适应；但不默认加入主公式，先判断是观察、验证还是动态分档。

本轮插问：用户要求回顾是否有验证事项未收录，以及待验证事项中是否有可以直接优化的内容。

处理原则：

- 先检查 `formula-discussion-summary.md / pending-verification-list.md / formula-changelog.md / current-checkpoint.md` 是否一致。
- 发现明显逻辑问题、命名问题、重复条件、断点过期，应直接优化或列为待确认直接优化项。
- 对阈值严松、边界权衡和副作用不确定的问题，仍放入验证清单。

本轮审计发现：

- 已直接修正：`current-checkpoint.md` 底部旧主线过期的问题。
- 已直接修正：`强普通黄候选原` 与新确认的 `黄可显示占用原` 记录冲突问题。
- 已确认直接优化：`平台普通强启` 中的 `AND (放额 OR 构上)` 与 `AND NOT(巨额 AND 明显长上)` 删除。
- 新发现待确认直接优化项：
  - 已确认：`平台突破特征` 中删除 `AND (温额 OR 构上)`，因为 `平台突破` 已包含 `构上`。
  - 已确认：`首扩强突贴轨` 中删除 `AND NOT(巨额 AND 长上)`，因为前面已有 `AND NOT(长上)`。
  - 已确认：删除 `前宽比2`，统一使用 `前宽比`。
  - 暂缓：`蓝候选原` 先不删除，等蓝/黄有效层和显示层全部讨论完，再统一判断是否还有引用。
  - `强普通黄候选原` 被 `黄可显示占用原` 替代后，主公式中不应再保留为正式变量，只保留旧版对比/验证字段。

## 外部沟通记录插问处理

用户提供 `C:/Users/Administrator/Desktop/沟通记录.docx`，说明旧 GPT 沟通存在验证过久、思路分散、但风险显示部分有可吸收价值。

处理结论：

- 已将该文档定位为参考材料，不改变当前主线和协作方法。
- 可吸收内容：
  - 单根K线诊断要先分层定位，不先猜；
  - 最终显示层应继续采用 0/1 归一化标记；
  - 后续显示层要复核强信号被弱信号或候选信号压制后的空窗问题；
  - 风险模块后续要重点审查高位创新高长上险、创新高冲回险、慎、滞的分层显示。
- 不直接吸收内容：
  - 旧记录中的跳跃式修改；
  - 围绕个别指数或个股形成的参数倾向；
  - 未按当前流程重新解释、评价、确认的公式改动。
- 已更新：
  - `collaboration-guidelines.md`：新增外部记录吸收原则、单根K线诊断链；
  - `validation-indicator-plan.md`：新增显示链诊断字段、风险提示诊断字段；
  - `pending-verification-list.md`：新增显示空窗、风险路径、慎/滞、成交额口径差异等验证项。

主线恢复位置不变：继续讨论 `启后保护N / 启后早期 / 启后冲高回落保护`。

## 待继续

继续讨论：

```pascal
启后保护N:=IF(周线,2,IF(月线,1,3));
启后早期:=BARSLAST(首扩启)>0 AND BARSLAST(首扩启)<=启后保护N;
启后趋势未坏:=C>短线 AND C>MID AND PCTB>0.75;
启后非极热:=PCTB<1+深限 AND NOT(高宽风险环境);
启后冲高回落保护:=启后早期 AND 启后趋势未坏 AND 启后非极热;
```

待讨论：继续拆 `启后冲高回落保护`。

## 待注意

- 后续要主动维护 `formula-discussion-summary.md`、`pending-verification-list.md`、`formula-changelog.md` 和本文件。
- 如果发现摘要文件中旧结论与后续确认结论不一致，应主动提出做一次“摘要一致性校验”。
- 不要在长上下文中只凭聊天记忆推进，优先以项目文件作为断点和决策来源。
## 最新断点：原公式逐块讨论已走到最终显示层

当前已经讨论并暂时接受：

- 高位放量创新高风险拆成 `可保护险` 与 `硬险`；
- `高位放量创新高硬险` 单独进入 `不可早期过滤硬险`，不再被 `初扩早期险过滤` 统一吞掉；
- `绝对硬险` 后续整理版改名为 `硬险源`；
- 最终 `险` 出口拆成 `不可早期过滤硬险 OR (普通险原 AND NOT(初扩早期险过滤))`；
- `慎源` 拆成 `慎源原 / 慎保护许可 / 慎源`；
- `滞弱化` 主公式暂不改，但验证副图增加 `滞强弱化 / 滞轻弱化`；
- 最终显示层统一加入 `显示许可`；
- 显示层继续使用 0/1 归一化，`险 / 上败` 显示改用 `险标=1 / 败标=1`；
- 蓝温、黄温继续只画细柱，不新增文字。

下一步建议：

1. 先做一次“阶段性总览”，确认目前所有已接受的直接优化；
2. 再进入“整理版公式生成前检查”：变量命名、重复条件、动态 REF、显示许可、无用变量删除；
3. 然后再决定是否开始输出整理版公式草案，或先继续逐块复核尚未完全讨论的模块。

## 最新状态：v1 草案已通过编译

用户已反馈：`formula-v1-draft.txt` 粘贴到东方财富通后没有报错。

当前含义：

- v1 草案语法层面初步成立；
- 已完成第一轮文本机械检查：无重复定义、无明确旧变量残留、无动态 `REF(PCTB,前5高点距)` 残留、括号与注释平衡；
- 下一步可以进入两条线：
  1. 继续逐块复核尚未完全讨论完的逻辑；
  2. 开始设计验证副图 v1，用来解释某根K线为什么出信号或不出信号，并辅助判断参数和逻辑是否合理。

后续提醒：

- 编译通过不等于逻辑最终正确；
- 后续必须比较原公式与 v1 在质量信号、风险信号、中性提示信号上的差异；
- 特别要看：旧公式有质量信号而 v1 漏掉、v1 新增信号是否合理、风险/慎/滞是否更准确。

## 最新确认：v2 需要补强高位放量创新高长上险

用户指出：创新高后留下长上影，如果上影线太长，或者股价近期涨幅过高，也不能被早期保护过滤。

已确认方向：

- 普通 `高位放量创新高长上险` 仍可作为可保护险；
- 但 `极端长上` 或 `高位涨幅过高` 的长上险，需要升级为硬险；
- v2 应新增 `高位放量创新高长上硬险`；
- `高位放量创新高硬险` 改为：`高位放量创新高冲回险 OR 高位放量创新高长上硬险`。

这属于风险结构补强，后续 v2 直接纳入，不只是验证项。

## 最新确认：v2 修正中位温上/中位暴上分界

v1 草案中仍为：

```pascal
中位温上:=中位收缩突破 AND PCTB<1.105;
中位暴上:=中位收缩突破 AND PCTB>=1.105;
```

用户已确认 v2 改为：

```pascal
中位温上:=中位收缩突破 AND PCTB<1.10;
中位暴上:=中位收缩突破 AND PCTB>=1.10;
```

影响：`PCTB` 在 `1.10` 到 `1.105` 之间的中位突破，会从中位温上改归中位暴上。

## 最新状态：验证副图第 1-6 区字段审计完成一轮

已按“规范到执行闭环”和“字段审计宁多不少”的规则，对验证副图第 1-6 区做了一轮字段完整性审计。

审计发现并已补入 `validation-indicator-plan.md`：

- 第 1 区已补充原始开高低收、涨跌幅、振幅、跳空幅度、盘中冲高回落比、实体高低、价格位置、成交额比例与量能阈值。
- 第 2 区已补齐布林带宽、启动前宽度、Keltner/挤压、有效挤压、迟发挤压、收缩链条。
- 第 3 区已补齐有效上穿、突破背景、温上/暴上/极上、中位突破、首扩、速、冲、蓝黄源头分层。
- 第 4 区补漏：`近上轨信号`、`近期强推`、`冷却占用信号原`、黄色显示/新鲜失败码、蓝黄速冲失败码。
- 第 5 区补漏：`不可早期过滤硬险`、`普通险原`、`慎源原`、`慎保护许可`、`滞强弱化`、`滞轻弱化`、风险源码/过滤码/慎滞码。
- 第 6 区补漏：`险显`、`败显`、`最终显示码`、各类未显示原因码、显示空窗、新旧公式最终信号组对照。

同时已补入 `pending-verification-list.md`：

- `高险跌坏冲回`
- `成交额分位 / 成交额历史百分位`
- `带宽自适应参考`
- `黄后确认候选`
- `温上承接弱`
- `慢推温上`
- `上败弱化候选`

下一步：向用户确认这一轮字段审计补漏是否接受，再决定是继续审计更细字段，还是开始讨论验证副图公式实现方式。

## 最新确认：D 图验证方式不只靠人工

用户指出“人工验证时”表述过窄，已确认 D 图后续验证应分三层：

- 人工看图验证：判断单根K线是否符合走势语境；
- Python 批量统计验证：导出最终显示码、原新版差异码、显示空窗码、未显示原因码，统计后续 3/5/10/20 根K线表现；
- 典型案例回放验证：对争议样本逐项回放源头、过滤、保护、显示和后续走势。

已同步更新：

- `validation-indicator-plan.md`
- `backtest-plan.md`

## 最新确认：验证副图实现优先级

用户已确认实现顺序：

```text
A → B → C → D
```

含义：

- A：基础 / 背景 / 源头；
- B：过滤 / 冷却 / 显示；
- C：确认 / 风险 / 慎滞；
- D：新旧公式对照 / 未显示原因。

后续按该顺序设计和实现验证副图。

## 最新确认：信息不足快速判定

用户已确认：诊断时不应等到连续 2-3 个变量缺失。只要有一个决定性变量缺失，就要进入信息不足判断。

规则：

- 一个决定性变量缺失，立即判定当前副图对该问题信息不足；
- 辅助变量缺失可以先继续，但要记录为待补字段；
- 如果缺字段导致无法判断信号成立/不成立是否合理，立即停止硬查，进入缺口分类；
- 不允许为了继续回答而凭感觉猜。

已同步写入：

- `validation-indicator-plan.md`
- `collaboration-guidelines.md`

## 最新状态：验证副图 A 草案已生成

已生成文件：

- `validation-indicator-A-draft.txt`
- `generate-validation-A.js`

当前 A 图定位：

- 基础 / 背景 / 源头；
- 用于先判断某根K线是否具备潜在信号基础；
- 覆盖基础K线、价格位置、成交额补充、带宽挤压收缩、启动源头。

已检查：

- A 图段落已生成；
- `中位温上 / 中位暴上` 已按用户确认修正为 `1.10` 分界；
- 文件中未残留 `1.105`。

下一步：

- 用户先把 `validation-indicator-A-draft.txt` 放入东方财富通编译；
- 若无报错，再进入 B 图：过滤 / 冷却 / 显示；
- 若有报错，先只处理 A 图兼容性，不急着继续 B/C/D。

## 最新状态：验证副图 A 显示修正版已生成

用户确认复制的是 `validation-indicator-A-draft.txt` 全文，但东方财富通显示效果不对：主要只看到 `PCTB` 线和少量挤压柱，没有看到完整验证标签。

判断：

- 不是复制文件错误；
- 属于 A 图显示层设计问题；
- 原 A 图太依赖密集 `DRAWTEXT`；
- 未强制副图显示范围，导致部分高低位置文字可能被裁切或压缩。

已生成显示修正版：

- `validation-indicator-A-readable-draft.txt`
- `generate-validation-A-readable.js`

修正版改动：

- 加入 `A顶:2.20` 和 `A底:-0.90` 强制显示范围；
- 用 `STICKLINE` 状态柱承载基础、背景、源头判断；
- 保留简短 `DRAWTEXT`，但不再只依赖文字；
- 不改变 A 图诊断逻辑。

下一步：

- 用户复制 `validation-indicator-A-readable-draft.txt` 到东方财富通测试显示；
- 如果仍不理想，再考虑进一步拆成 A1/A2/A3 三个更小副图，或改成数字状态码版。

## 最新状态：A 图 v2 仍未显示，新增显示冒烟测试

用户确认 `validation-indicator-A-readable-draft.txt` 仍然没有显示预期内容。截图显示副图坐标仍约为 `1.30 / -0.30`，说明 `A顶:2.20` 和 `A底:-0.90` 没有参与坐标显示。

当前判断：

- 不能继续盲目修改完整 A 图；
- 需要先确认东方财富通副图是否能显示固定线、固定柱、固定文字；
- 如果极简显示测试能显示，问题大概率来自 A 图输出项太多、后置输出未参与显示、或公式复杂度/显示限制；
- 如果极简显示测试也不能显示，需要检查公式类型、导入方式、显示设置或平台兼容性。

已新增：

- `validation-display-smoke-test.txt`

用途：

- 只测试固定线、固定柱、固定文字能否显示；
- 不涉及主公式逻辑；
- 用于定位是平台显示问题，还是 A 图复杂度问题。

## 最新状态：显示冒烟测试通过，生成 A 独立显示版

用户测试 `validation-display-smoke-test.txt` 后，固定线、红色柱子和文字均能显示，说明：

- 东方财富通可以显示 `A顶/A底` 这类固定输出；
- 可以显示 `STICKLINE`；
- 可以显示 `DRAWTEXT`；
- 问题不在平台基本绘图函数。

进一步判断：

- 原 A 图没有显示，主要可能是因为继承了原公式前面的输出项，例如 `PCTB:`、`超买:`、`中轴:` 等；
- 东方财富通可能优先显示前面输出项，后面追加的验证输出没有进入显示坐标或被限制；
- 因此验证副图不应直接继承原公式输出，而应把原输出改为内部变量，只在最后输出验证图需要的线和状态柱。

已生成：

- `validation-indicator-A-standalone-draft.txt`
- `generate-validation-A-standalone.js`

独立版处理：

- `PCTB:` 改为 `PCTB:=`；
- `超买 / 中轴 / 超卖 / 极上线 / 极下线` 改为内部变量；
- 删除原挤压显示；
- 最后只输出 `A顶 / A底 / PCTB线` 和 A 图状态柱；
- 目标是避免原公式输出项抢占验证图显示。

## 最新状态：A 独立版报“至少要有一个输出项”，生成提前输出版

用户测试 `validation-indicator-A-standalone-draft.txt` 后，东方财富通报错：公式至少要有一个输出项。

判断：

- 该独立版虽然最后有 `A顶 / A底 / PCTB线` 输出，但输出项太靠后；
- 东方财富通可能不接受前面大量内部变量、最后才输出的结构；
- 验证副图需要在较前位置就出现输出项。

已生成：

- `validation-indicator-A-early-output-draft.txt`
- `generate-validation-A-early-output.js`

处理方式：

- 第48行仍为内部计算：`PCTB:=...`；
- 第51-53行提前输出：`A顶 / A底 / PCTB线`；
- 原 `超买 / 中轴 / 超卖 / 极上线 / 极下线` 继续作为内部变量；
- 后面只追加 A 图状态柱和文字。

下一步：

- 用户复制 `validation-indicator-A-early-output-draft.txt` 测试；
- 如果通过，B/C/D 后续也采用“提前输出 + 后置状态柱”的结构。

## 最新状态：A 提前输出版可显示核心线，需继续定位后置状态柱

用户测试 `validation-indicator-A-early-output-draft.txt` 后，`A顶 / A底 / PCTB线` 已正常显示，说明提前输出结构有效。

当前剩余问题：

- A 图状态柱和状态文字仍未明显显示；
- 不能直接断定是条件未成立，还是后置 `STICKLINE / DRAWTEXT` 未参与显示；
- 需要用固定条件测试后置显示能力。

已生成：

- `validation-indicator-A-early-output-debug-draft.txt`
- `generate-validation-A-early-output-debug.js`

用途：

- 在 A 图末尾追加固定条件 `A后置测试:=C>0`；
- 若能显示红柱和 `A测`，说明后置显示函数可用，状态柱不显示主要是条件未触发或太稀疏；
- 若不能显示，说明东方财富通对后置绘图仍有限制，A 图需要改成“所有输出提前定义”或拆分为更小副图。

## 最新状态：A 图决定拆分为短公式测试

用户测试 debug 版后，`A后置测试` 仍未显示。由此确认：

- 后置绘图语句放到完整公式末尾时，东方财富通没有正常显示；
- 不能继续把完整 A 图堆在一个 700 行左右的公式里；
- 验证副图需要拆分，或者让绘图语句更早出现。

已生成：

- `validation-indicator-A1-basic-draft.txt`
- `validation-indicator-A2-background-draft.txt`
- `validation-indicator-A3-source-draft.txt`
- `generate-validation-A-split.js`

当前拆分：

- A1：基础K线 / 价格位置 / 成交额；
- A2：挤压 / 收缩 / 宽度背景。
- A3：启动源头 / 蓝黄来源。

生成原则：

- 保留提前输出 `图顶 / 图底 / PCTB线`；
- 状态柱控制在约 200 行附近出现；
- 先确认 A1/A2 能否正常显示；
- A3 源头信号最复杂，等 A1/A2 显示确认后再单独瘦身生成。

补充：

- A1 / A2 已经是短公式，适合优先测试；
- A3 目前已生成，但因为需要计算到蓝黄源头，行数仍偏长，状态显示仍可能受东方财富通后置绘图限制；
- 如果 A3 显示不理想，后续应优先改成“源头码紧凑版”或继续拆分为更小源头专项图，而不是让用户日常同时看很多副图。

## 最新状态：A1 可显示核心线，但状态柱仍需定位

用户展示 A1 副图后，已看到 `图顶 / 图底 / PCTB线`，说明 A1 能编译并显示核心输出。

注意：

- 用户截图处于分时页面，不是日线/周线/月线；
- 本公式主要为日线/周线/月线设计，分时结果不能作为有效性判断依据；
- A1 状态柱和文字在截图中未明显显示，需要区分是分时/条件原因，还是后置绘图问题。

已新增：

- `validation-indicator-A1-basic-debug-draft.txt`
- `generate-validation-A1-debug.js`

用途：

- 在 A1 末尾加入固定成立的 `A1固定测试:=C>0`；
- 若日线下能显示 `A1测` 红柱，说明 A1 后置绘图可用；
- 若日线下仍不显示，说明即使短公式末尾绘图也不稳定，需要把 A1 状态柱进一步提前。

## 最新状态：A1 debug 仍不显示，生成紧跟输出测试

用户测试 `validation-indicator-A1-basic-debug-draft.txt` 后，仍未看到 `A1测`，说明：

- `A1固定测试:=C>0` 永远成立，但后置绘图仍未显示；
- 问题不是条件未触发；
- 东方财富通在该公式结构中不稳定显示后置 `STICKLINE / DRAWTEXT`。

已生成：

- `validation-indicator-A1-immediate-debug.txt`
- `generate-validation-A1-immediate-debug.js`

用途：

- 只保留到 `PCTB` 计算；
- 在 `图顶 / 图底 / PCTB线` 后立刻加入固定绘图 `早测`；
- 用于确认绘图是否必须紧跟早期输出项。

下一步：

- 用户测试 `validation-indicator-A1-immediate-debug.txt`；
- 如果 `早测` 能显示，说明绘图位置必须尽量靠前；
- 如果 `早测` 也不能显示，则需要重新检查公式类型或该软件对当前公式结构的限制。

## 最新状态：A1 前置绘图正式版已生成

用户测试 `validation-indicator-A1-immediate-debug.txt` 后，`早测` 红柱和文字能显示。由此确认：

- `STICKLINE / DRAWTEXT` 本身可用；
- 绘图语句紧跟早期输出时可显示；
- 后续验证副图需要采用“变量就近计算 + 就近绘图”的结构。

已生成：

- `validation-indicator-A1-front-draft.txt`
- `generate-validation-A1-front.js`

特点：

- 不再从完整主公式截取几百行；
- 只保留 A1 必需变量：周期、PCTB、K线质量、成交额；
- 状态柱从约第70行开始；
- 用于测试 A1 基础诊断能否稳定显示。

显示含义：

- `阳`：阳强；
- `中`：强收中阳候选；
- `上`：长上影；
- `温 / 放 / 巨`：成交额级别；
- `昨 / 量`：相对昨日、相对前5高量放量。

## 最新状态：A1 前置版可显示，但偏花，生成精简版

用户测试 `validation-indicator-A1-front-draft.txt` 后反馈“有点花”。

原因：

- 连续数值线过多：`实占线 / 上影线 / 收盘位线 / 额比线` 与 `PCTB线` 互相交错；
- 高频文字过多：`阳 / 温 / 放` 等在很多K线上连续显示；
- 日常可读性不足。

处理原则：

- 不删除诊断能力，而是分层使用；
- `A1-front` 保留为细节版；
- 新增 `A1-clean` 作为日常可读版。

已生成：

- `validation-indicator-A1-clean-draft.txt`
- `generate-validation-A1-clean.js`

精简版显示结构：

- 保留 `PCTB线`；
- 删除连续的 `实占线 / 上影线 / 收盘位线 / 额比线`；
- 低区显示上影风险；
- 中区显示实体质量；
- 高区显示量能；
- 只给关键情况打文字，不再每根都密集标注。

## 最新状态：A2 精简版已生成

在 A1-clean 可读性改善后，已按同样风格生成 A2-clean。

已生成：

- `validation-indicator-A2-clean-draft.txt`
- `generate-validation-A2-clean.js`

A2-clean 用途：

- 判断挤压背景；
- 判断迟发挤压背景；
- 判断收缩背景；
- 判断启动前宽度是否许可。

显示结构：

- 低区：挤压背景；
- 中区：收缩背景；
- 高区：迟发挤压 / 启动前持宽 / 宽近高 / 首扩宽度许可。

下一步：

- 用户测试 A2-clean；
- 如果显示可读，再继续做 A3-clean；
- 如果偏花，继续按 A1-clean 思路压缩文字和线条。

## 最新状态：A2-clean 迟发绝宽线语法修复

用户测试 A2-clean 时，东方财富通报错，定位到 `迟发绝宽线` 的多行嵌套 `IF`。

原写法问题：

- `迟发绝宽线` 使用多行嵌套 `IF`；
- 东方财富通在该位置解析不稳定；
- 逻辑本身没问题，但写法需要兼容化。

已修复：

```pascal
迟发基准绝宽线:=IF(周线,0.35,IF(月线,0.45,0.22));
迟发绝宽线:=IF(高位2年,迟发基准绝宽线*0.90,IF(低位2年,迟发基准绝宽线*1.10,迟发基准绝宽线));
```

含义：

- 先确定基础迟发绝宽线；
- 高位两年位置更严格，乘以 `0.90`；
- 低位两年位置稍放宽，乘以 `1.10`；
- 中位保持基础值。

已重新生成：

- `validation-indicator-A2-clean-draft.txt`

## 最新状态：A2-clean-v2 生成，避免旧标签缓存和嵌套 IF

用户再次看到同一段旧报错。经本地核对，`validation-indicator-A2-clean-draft.txt` 当前第80行已经不是报错上下文，且旧的多行写法已经不存在。大概率是打开的旧文件标签未刷新或复制了旧内容。

为避免混淆，已生成新文件名：

- `validation-indicator-A2-clean-v2-draft.txt`
- `generate-validation-A2-clean-v2.js`

v2 进一步拆开迟发绝宽线：

```pascal
迟发基准绝宽线:=IF(周线,0.35,IF(月线,0.45,0.22));
迟发高位绝宽线:=迟发基准绝宽线*0.90;
迟发低位绝宽线:=迟发基准绝宽线*1.10;
迟发非低位绝宽线:=IF(高位2年,迟发高位绝宽线,迟发基准绝宽线);
迟发绝宽线:=IF(低位2年,迟发低位绝宽线,迟发非低位绝宽线);
```

目的：

- 避免旧文件缓存；
- 避免多行嵌套 IF；
- 避免报错上下文继续指向旧写法。

## 最新状态：A2-clean-v3 提前输出以修复无输出项报错

用户测试 A2-clean-v2 后出现“公式至少要有一个输出项”。判断原因：

- A2-clean-v2 的第一个输出项仍然较靠后；
- 东方财富通可能在前段没有遇到输出项时提前判定公式无输出；
- A1 成功经验是：较早位置就输出 `图顶 / 图底 / PCTB线`。

已生成：

- `validation-indicator-A2-clean-v3-draft.txt`
- `generate-validation-A2-clean-v3.js`

v3 处理：

- 在 `PCTB` 计算后立即输出：
  - `图顶`
  - `图底`
  - `PCTB线`
- 后面保留 `宽比线` 和 A2 状态柱；
- 不改变 A2 逻辑，只调整输出位置。

## 最新状态：确认“算完马上画”，A2-front 已生成

用户明确要求：既然已经知道东方财富通喜欢“算完马上画”，后续就应默认这么做。

已入规：

- `collaboration-guidelines.md`
- `validation-indicator-plan.md`

已生成：

- `validation-indicator-A2-front-draft.txt`
- `generate-validation-A2-front.js`

A2-front 结构：

- PCTB 与挤压算完后，马上画挤压区；
- 带宽与启动前宽度算完后，马上画宽度区；
- 有效挤压算完后，马上画有挤；
- 迟发挤压算完后，马上画迟发；
- 收缩算完后，马上画真缩。

注意：

- A2-front 文件约 140 行；
- 收缩显示仍然在较后位置，若不显示，下一步拆成 A2a 挤压/宽度、A2b 收缩；
- 但先测试 A2-front。

## 最新状态：A2-readable 小修已生成

用户选择先做 A2-clean-readable 小修。

已生成：

- `validation-indicator-A2-readable-draft.txt`
- `generate-validation-A2-readable.js`

小修内容：

- 保留 `启动前持宽` 和 `宽近高` 的灰色柱子；
- 删除 `持` 和 `近高` 的重复文字；
- 保留关键背景文字：
  - `有挤`
  - `迟`
  - `真缩`

目的：

- 降低高频过滤文字干扰；
- 保留限制条件的可视痕迹；
- 让 A2 日常看图更清爽。

## 最新状态：A3-clean 源头简化版已生成

进入 A3-clean：启动源头 / 蓝黄来源。

已生成：

- `validation-indicator-A3-clean-draft.txt`
- `generate-validation-A3-clean.js`

定位：

- 只看源头层；
- 不包含完整冷却、最终显示和风险过滤；
- 用于判断启动迹象来自哪里。

显示结构：

- 低区：突破背景 / 蓝优质背景；
- 中区：有效上穿 / 温上 / 暴上 / 极上；
- 高区：中位收缩突破 / 首扩候选 / 蓝源 / 黄源。

注意：

- A3-clean 约 195 行，比 A1/A2 复杂；
- 若显示不完整，下一步拆成：
  - A3a：上穿 / 温暴极；
  - A3b：首扩 / 蓝黄来源。

## 最新状态：A3a 上穿/温暴极拆分版已生成

用户测试 `A3-clean` 后，发现只稳定显示 `PCTB线 / 宽比线`，源头层文字和柱子没有完整显示。

处理方式：

- 不继续在 A3-clean 上硬塞；
- 按“算完一组变量 -> 马上画一组”的东方财富通兼容规则；
- 将 A3 拆成更小的 A3a / A3b。

已生成 A3a：

- `validation-indicator-A3a-upbreak-draft.txt`
- `generate-validation-A3a-upbreak.js`

A3a 用途：

- 只诊断普通上穿路径；
- 看突破背景、有效上穿、温上、暴上、极上、暴上险、极上险；
- 不处理首扩、蓝黄来源、冷却、最终显示。

下一步：

- 先让用户把 `validation-indicator-A3a-upbreak-draft.txt` 放进软件编译和观察；
- 如果 A3a 显示正常，再做 A3b：首扩 / 蓝黄来源；
- 如果 A3a 仍然偏花，再继续做 A3a-readable 小修。

## 最新状态：A3a-debug 上穿路径断点版已生成

用户测试 A3a 后，日线下仍主要显示 `PCTB线 / 宽比线`，缺少明显的上穿/温暴极文字和柱子。

为避免继续猜测，已生成 A3a-debug：

- `validation-indicator-A3a-debug-draft.txt`
- `generate-validation-A3a-debug.js`

A3a-debug 只检查关键断点：

- `显示许可`
- `突破背景`
- `上穿1原`
- `有效上穿1`
- `上穿源原`
- `上穿源成`

下一步：

- 用户先把 A3a-debug 放进软件；
- 如果灰色底柱有，但没有绿色/白色/黄色/红色，说明不是显示问题，而是这些断点条件确实没触发；
- 如果断点触发但不显示，再继续调整绘图方式；
- 如果 A3a-debug 证明普通上穿路径本来就不常触发，下一步转入 A3b：首扩 / 蓝黄来源。

## 最新结论修正：A3a 普通上穿路径有触发

用户更正：刚才表达反了，A3a-debug 中普通上穿路径是有触发的。

已修正结论：

- 底部灰色 `显示许可` 已显示，说明周期许可与绘图正常；
- 普通上穿路径关键断点有触发；
- 因此不能说当前区段主要不是 A3a 路径；
- 后续需要继续看触发的是哪一层：`突破背景`、`上穿1原`、`有效上穿1`、`上穿源原`、还是最终 `温上/暴上/极上/风险源`。

下一步：

- 先不要急着跳到 A3b；
- 应先根据 A3a-debug 的颜色/文字确认具体触发层级；
- 若 A3a-debug 能解释这段走势，则继续做 A3a-readable 或 A3a-final；
- 若只触发很底层、没有进入最终源头，再决定是否转入 A3b。

补充：

- 已额外生成 `validation-indicator-A3a-crosscheck-draft.txt`，用于必要时核对 `PCTB>1`、手工上穿与 `CROSS(PCTB,1)` 是否一致。

## 最新状态：A3a-readable 日常可读版已生成

基于用户更正：A3a-debug 中普通上穿路径有触发。

已生成：

- `validation-indicator-A3a-readable-draft.txt`
- `generate-validation-A3a-readable.js`

定位：

- 不是 debug 断点版，而是日常可读版；
- 用于观察普通上穿路径的贡献；
- 保留关键层级，减少底部许可柱和高频文字。

读法：

- 绿色：突破背景；
- 白色：原始上穿或源头原始层；
- 黄色：有效上穿或温上；
- 红色：暴上；
- 紫色：极上或暴险/极险。

下一步：

- 用户先测试 A3a-readable；
- 如果可读性合适，A3a 暂时收敛；
- 然后进入 A3b：首扩 / 蓝黄来源。

## 最新状态：A3a-debug-selfcheck 已生成

用户指出：A3a-debug 也有问题，只显示灰柱。

重要修正：

- 灰柱只证明最前面的 `显示许可` 绘图正常；
- 不能据此判断后面的条件没有触发；
- 因为后面的 `STICKLINE / DRAWTEXT` 仍可能被东方财富通后段绘图问题影响。

已生成：

- `validation-indicator-A3a-debug-selfcheck-draft.txt`
- `generate-validation-A3a-debug-selfcheck.js`

这个版本加入三个无条件后段绘图自检：

- `后1`：背景段；
- `后2`：上穿段；
- `后3`：源头段。

判断：

- 如果 `后1/后2/后3` 都不显示，说明是后段绘图问题，不是条件没触发；
- 如果蓝色后段自检显示，但其他颜色不显示，才说明条件没触发。

## 最新状态：A3a1-upcross-mini 已生成

用户测试 A3a-debug-selfcheck 后，截图仍只显示最前面的灰色许可柱，没有看到 `后1/后2/后3`。

结论：

- A3a 这种长图后段绘图不可靠；
- 不能再用长图判断普通上穿路径；
- 需要拆成真正短小的子图。

已生成：

- `validation-indicator-A3a1-upcross-mini-draft.txt`
- `generate-validation-A3a1-upcross-mini.js`

A3a1 只检查：

- `PCTB>1`
- 手工上穿
- `CROSS(PCTB,1)`
- 浅绕回过滤
- 有效上穿

下一步：

- 用户先测试 A3a1；
- 如果 A3a1 显示正常，再继续做 A3a2：突破背景短图；
- 最终不要再强行把 A3a 全部塞进一张长副图。

## 最新状态：A3a2-break-bg-mini 已生成

用户要求继续，进入普通上穿路径的第二层：突破背景。

已生成：

- `validation-indicator-A3a2-break-bg-mini-draft.txt`
- `generate-validation-A3a2-break-bg-mini.js`

A3a2 只检查：

- 当根 `挤压`
- `背景近挤`
- `迟发挤压背景`
- `背景昨缩`
- `背景今缩`
- `突破背景` 总开关

读法：

- 紫色：挤压；
- 白色：近挤背景；
- 粉色：迟发；
- 青色：昨缩；
- 绿色：今缩；
- 红色：突破背景。

下一步：

- 用户先测试 A3a2；
- 如果 A3a1 有有效上穿、A3a2 有红色背景，但仍没有普通源头，则做 A3a3：温暴极质量短图。

## 长期锚点：主公式与验证副图必须同步

用户新增 `记锚`：

- 后续对主公式做任何调整时，都必须同步想到如何调整所有验证副图；
- 目标是让验证副图始终和主公式口径对应；
- 不能让副图按旧条件解释新公式。

已新增文件：

- `long-term-anchors.md`

执行要求：

- 每次主公式变量、条件、阈值、窗口或显示优先级变化时，检查 A/B/C/D 验证副图；
- 同步检查所有当前仍被采用、有效、最新定稿或准定稿的验证副图版本，不限于 `mini/debug/readable` 这几个命名；
- 必要时同步更新回测字段、对比表、版本记录和读法说明；
- 如果某张副图暂时不同步，必须标注它已经落后于主公式，不能用于最终判断。

## 最新状态：A3a3-upbreak-quality-mini 已生成

进入普通上穿路径第三层：温暴极质量结果。

已生成：

- `validation-indicator-A3a3-upbreak-quality-mini-draft.txt`
- `generate-validation-A3a3-upbreak-quality-mini.js`

A3a3 用途：

- A3a1 看是否有效上穿；
- A3a2 看是否有突破背景；
- A3a3 看是否进入 `温上 / 暴上 / 极上 / 暴上险 / 极上险`。

A3a3 重点显示：

- `阳强`
- `长上`
- `温额 / 放额 / 巨额`
- `构上`
- `源原`
- `温上 / 暴上 / 极上 / 暴上险 / 极上险`

下一步：

- 用户测试 A3a3；
- 如果能正常显示，普通上穿路径拆分版 A3a1/A3a2/A3a3 基本可用；
- 之后进入 A3b：首扩 / 蓝黄来源。

## 协作规则更新：长上下文切换与交接

用户确认 `入规`：

- 当上下文已经明显过长，且项目已经不是普通聊天而是长期公式项目时，助手应主动建议新开对话；
- 如果继续硬撑可能造成卡顿、遗漏、反应变慢，应优先用项目文件交接恢复；
- 新开对话前应准备或更新交接文件，例如 `new-chat-handoff.md`；
- 新对话开始后，优先读取项目文件恢复状态，而不是让用户重新解释全部历史。

已写入：

- `collaboration-guidelines.md`

当前建议：

- 如果继续公式讨论，建议新开对话后先读取 `new-chat-handoff.md` 和其中列出的必读文件；
- 当前主线仍是：A3a 普通上穿路径拆分版已完成，下一步进入 A3b：首扩 / 蓝黄来源。

## 最新状态：A3b1-first-expand-mini 已生成

进入 A3b：首扩 / 蓝黄来源中的第一张短图。

已生成：

- `validation-indicator-A3b1-first-expand-mini-draft.txt`
- `generate-validation-A3b1-first-expand-mini.js`

A3b1 用途：

- 判断首扩来源是否成立；
- 核对 `首扩宽度许可`、`真正收缩`、`上轨趋势`、`带宽初扩`、`高位平台再突`、`首扩候选`、`首扩周期许可`、`首扩启`；
- 属于 A 图源头层，不处理蓝黄最终来源、冷却、风险和最终显示。

显示读法：

- `宽许`：启动前宽度许可；
- `真缩`：真正收缩；
- `趋`：上轨趋势；
- `初扩`：带宽初扩；
- `平热`：高位平台再突阻断；
- `候`：首扩候选；
- 白柱：首扩周期许可；
- 红粗柱 / `首扩`：首扩启。

注意：

- A3b1 约 236 行，比 A3a mini 更长，因为首扩链必须带上收缩、宽度、趋势、平台阻断和周期许可；
- 若用户测试后发现后段仍不稳定，应继续拆成更短的 A3b1a / A3b1b，而不是继续加长本图。

下一步：

- 用户测试 `validation-indicator-A3b1-first-expand-mini-draft.txt`；
- 如果显示稳定，再进入 `A3b2：蓝黄来源`。
